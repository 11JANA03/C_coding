#include "stdio.h"
int main()
{
    int M,N;
    int a[100],sum;
    scanf("%d %d",&M,&N);
    for(int i=0;i<M*N;i++)
    {
        scanf("%d",&a[i]);
        if(a[i]>0)
        {
            sum+=a[i];
        }
    }
    printf("%d",sum);
    return 0;
}
